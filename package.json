{
  "name": "cactus",
  "version": "0.1.0",
  "description": "Cactus Framework and Utilities Monorepo",
  "main": "index.js",
  "repository": "git@github.com:repaygithub/cactus.git",
  "contributors": [
    "Dhalton",
    "JamesNimlos"
  ],
  "scripts": {
    "w": "yarn workspace",
    "ws": "yarn workspaces",
    "i18n": "yarn workspace @repay/cactus-i18n",
    "fwk": "yarn workspace @repay/cactus-fwk",
    "web": "yarn workspace @repay/cactus-web",
    "theme": "yarn workspace @repay/cactus-theme",
    "icons": "yarn workspace @repay/cactus-icons",
    "lint": "eslint \"**/*.{ts,tsx,js,jsx}\"",
    "fmt": "yarn lint --fix",
    "test": "yarn test:ci",
    "test:ci": "yarn lint && yarn ws run test:ci",
    "build": "yarn fwk build && yarn theme build && yarn icons build && yarn i18n build",
    "predocs:publish": "./scripts/build-storybooks.js",
    "docs:publish": "gh-pages -d ./dist",
    "cleanup": "rm -rf dist && yarn ws run cleanup"
  },
  "license": "MIT",
  "private": true,
  "workspaces": {
    "packages": [
      "modules/*",
      "examples/*"
    ],
    "noHoist": [
      "**/jest-styled-components"
    ]
  },
  "devDependencies": {
    "@babel/core": "^7.3.4",
    "@repay/eslint-config": "^1.1.0",
    "babel-eslint": "^10.0.1",
    "cpy": "^7.2.0",
    "eslint": "^5.14.1",
    "gh-pages": "^2.0.1",
    "prettier": "^1.16.4",
    "typescript-eslint-parser": "^22.0.0"
  }
}
